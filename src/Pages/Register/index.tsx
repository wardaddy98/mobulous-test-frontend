import LoginRegister from '../../components/LoginRegister';

const Register = () => {
  return (
    <LoginRegister
      type='register'
      description='Create new account today to reap the benefits of a personalized experience.'
    />
  );
};

export default Register;
