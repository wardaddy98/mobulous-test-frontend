import LoginRegister from '../../components/LoginRegister';

const Login = () => {
  return <LoginRegister type='login' description='Welcome back! Sign in to your account.' />;
};

export default Login;
